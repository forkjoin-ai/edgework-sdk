# transport

## Purpose
This directory (`packages/edgework-sdk/src/agent/transport`) contains modules, components, and logic specific to the transport domain within the Affectively monorepo.

## Key Files
- `index.ts`: Core asset for transport.
- `p2p.ts`: Core asset for transport.
- `a2a.ts`: A2A client/server protocol with Gnosis runtime orchestration for discovery/task dispatch.

## Usage
Refer to the internal exports and local tests for usage examples. Integrated via standard workspace imports.
