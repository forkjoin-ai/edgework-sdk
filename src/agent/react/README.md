# react

## Purpose
This directory (`packages/edgework-sdk/src/agent/react`) contains modules, components, and logic specific to the react domain within the Affectively monorepo.

## Key Files
- `use-agent-stream.ts`: Core asset for react.
- `use-agent.ts`: Core asset for react.
- `agent-provider.tsx`: Core asset for react.
- `index.ts`: Core asset for react.

## Usage
Refer to the internal exports and local tests for usage examples. Integrated via standard workspace imports.
