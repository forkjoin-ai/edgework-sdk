# eval

## Purpose
This directory (`packages/edgework-sdk/src/agent/eval`) contains modules, components, and logic specific to the eval domain within the Affectively monorepo.

## Key Files
- `evaluator.ts`: Core asset for eval.
- `index.ts`: Core asset for eval.

## Usage
Refer to the internal exports and local tests for usage examples. Integrated via standard workspace imports.
