# backends

## Purpose
This directory (`packages/edgework-sdk/src/agent/memory/backends`) contains modules, components, and logic specific to the backends domain within the Affectively monorepo.

## Key Files
- `memory-backend.ts`: Core asset for backends.
- `indexeddb-backend.ts`: Core asset for backends.

## Usage
Refer to the internal exports and local tests for usage examples. Integrated via standard workspace imports.
