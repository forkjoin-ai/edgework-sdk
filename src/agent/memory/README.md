# memory

## Purpose
This directory (`packages/edgework-sdk/src/agent/memory`) contains modules, components, and logic specific to the memory domain within the Affectively monorepo.

## Key Files
- `memory-service.ts`: Core asset for memory.
- `backends`: Core asset for memory.
- `index.ts`: Core asset for memory.

## Usage
Refer to the internal exports and local tests for usage examples. Integrated via standard workspace imports.
