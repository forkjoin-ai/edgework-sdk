# mcp

## Purpose
This directory (`packages/edgework-sdk/src/agent/mcp`) contains modules, components, and logic specific to the mcp domain within the Affectively monorepo.

## Key Files
- `client.ts`: MCP client adapter now backed by the Gnosis runtime/fetch compat layer.
- `index.ts`: Core asset for mcp.
- `server.ts`: Core asset for mcp.

## Usage
Refer to the internal exports and local tests for usage examples. Integrated via standard workspace imports.
