# edge

## Purpose
This directory (`packages/edgework-sdk/src/agent/edge`) contains modules, components, and logic specific to the edge domain within the Affectively monorepo.

## Key Files
- `peer-agent.ts`: Core asset for edge.
- `edge-agent.ts`: Core asset for edge.
- `index.ts`: Core asset for edge.

## Usage
Refer to the internal exports and local tests for usage examples. Integrated via standard workspace imports.
