# esi-pool

## Purpose
This directory (`packages/edgework-sdk/src/esi-pool`) contains modules, components, and logic specific to the esi-pool domain within the Affectively monorepo.

## Key Files
- `ESIPool.tsx`: Core asset for esi-pool.
- `useESIPool.ts`: Core asset for esi-pool.
- `index.ts`: Core asset for esi-pool.

## Usage
Refer to the internal exports and local tests for usage examples. Integrated via standard workspace imports.
